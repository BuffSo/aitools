# AI TOOLS 공식 홈페이지 — SPEC

> 출처: `ref/AITools_홈페이지기획서_v2.docx` (v2.0, 시안 기반 확정본) 및 사용자 확정 결정.
> 본 스펙은 **개발용 단일 진실 소스(Single Source of Truth)** 입니다. 변경 시 이 문서를 먼저 갱신합니다.

---

## 1. 목적 (Objective)

**무엇을**: ㈜에이아이툴즈(AI TOOLS)의 공식 마케팅 홈페이지. AI 기반 온라인 광고 성과 분석·운영 자동화 플랫폼(제품명 AdBrain)을 소개하고 **데모 신청 리드를 수집**한다.

**누구를 위해**: 광고 운영 효율화를 원하는 기업의 마케팅 담당자·대표(B2B). 부차적으로 투자자·파트너·채용 지원자.

**핵심 성공 지표**: 데모 신청 전환(Primary CTA), 솔루션 소개서 다운로드(Secondary CTA).

**이번 범위 (1차 — 메인 랜딩)**:
- 메인 홈(`/`) 1페이지 완성: **Hero + 섹션01~04 + 푸터**.
- GNB(7개 메뉴) — 메인의 앵커/플레이스홀더 라우트로 연결, 페이지 본문은 2차.
- 데모 신청 폼 — **UI + 클라이언트 검증까지만**, 실제 전송(이메일/DB) 연동은 2차.

**이후 범위 (2차+, 14주 로드맵)**: `/service` `/solution` `/patent` `/about` `/news` `/contact` `/demo` `/login`, 소개서·특허 PDF 다운로드, 데모 폼 백엔드 연동, 카카오맵(오시는 길).

---

## 2. 브랜드 & 디자인 토큰 (확정)

| 항목 | 값 |
|---|---|
| 한글명 | 에이아이툴즈 |
| 영문명 | AI TOOLS / AI TOOLS Co., Ltd. |
| 슬로건 | 인공지능 기술로 마케팅의 새로운 가능성을 열고, 고객의 성장을 함께 만들어가는 AI 마케팅 파트너 |
| 도메인 | **aitoolz.kr** (non-www 정식 / `www.aitoolz.kr` → `aitoolz.kr` 301 리다이렉트) |
| 이메일 | **aitoolz@kakao.com** |
| 로고 | 원본 `ref/aitools_logo.jpeg`. 가공본 `assets/`: `logo-horizontal.png`(흰 텍스트·다크용)·`logo-horizontal-dark.png`(네이비 텍스트·라이트용)·`logo-icon.png`(아이콘, 투명). 파비콘 세트 `assets/favicon*`·`apple-touch-icon.png` |

**색상 시스템** (Tailwind 테마 토큰으로 등록):

| 토큰 | Hex | 용도 |
|---|---|---|
| `primary` | `#1749A6` | 로고·헤더·주요 버튼·제목 |
| `secondary` | `#2D7DD2` | CTA·링크·아이콘 강조 |
| `ai-glow` | `#00B4D8` | AI 다이어그램 글로우·포인트 |
| `bg-light` | `#F0F4FF` | 섹션/카드 배경 |
| `text-dark` | `#1A1A2E` | 본문·헤드라인 |
| `border` | `#E2E8F0` | 카드 테두리·구분선 |

> 디자인 시안 PNG는 없음. 위 토큰·카피·레이아웃 규칙을 근거로 **깔끔한 현대적 SaaS 랜딩**으로 제작한다 ("AI 생성물처럼 보이지 않게" — frontend-ui-engineering 원칙 적용).

---

## 3. 페이지 구성 (메인 홈)

번호형 섹션 구조: **Hero → 01 → 02 → 03 → 04 → Footer**.

### GNB (헤더)
- 좌: 육각형 심볼 + `AI TOOLS` 워드마크
- 중앙: `홈 · 서비스 · 솔루션 · 특허/기술 · 회사소개 · 뉴스 · 문의하기` (7개)
- 우: `로그인`(텍스트) + `데모 신청`(Primary 파란 버튼)
- 동작: 스크롤 50px↑ 시 배경 투명→흰색 + 그림자 고정. 모바일은 햄버거 → 전체 드로어.

### Hero
- 태그: `AI MARKETING OS` (좌상단 '01' 넘버 옆)
- 헤드라인: **"AI가 광고의 성과를 분석하고 의사결정합니다"** (`분석하고` 뒤 줄바꿈)
- 서브: "에이아이툴즈는 인공지능으로 광고의 성과를 분석하고, 의사결정하여, 최적의 광고 운영 솔루션을 제공합니다."
- CTA1: `데모 신청하기 →` (파란 배경) · CTA2: `솔루션 소개서 다운로드 ↓` (흰 배경+파란 테두리)
- 연동 플랫폼 배지: Google Ads / Meta / NAVER / kakao
- 레이아웃: 데스크톱 2단(좌 텍스트·우 AI 다이어그램), 모바일 1단(텍스트 상·이미지 하)

### 섹션 01 — AI 기능 다이어그램
중앙 'AI' 칩(파란 배경 + 회로 글로우) + 6방향 방사형 기능 카드:
좌상 **데이터 분석** / 좌중 **예산 최적화** / 좌하 **자동 실행** / 우상 **타겟팅 최적화** / 우중 **소재 생성** / 우하 **성과 리포트**.
연결선 스크롤 진입 시 순차 페이드인. 모바일: 2열 그리드 + 중앙 AI 칩.

### 섹션 02 — 핵심 기능 6종 (3×2 그리드)
제목: "AI가 모든 과정을 자동으로 처리합니다"

| # | 기능 | 카피 | 링크 |
|---|---|---|---|
| 1 | AI 예산 최적화 | 광고 성과를 실시간 분석하여 최적의 예산을 자동 배분 | `/service/budget` |
| 2 | AI 광고 소재 생성 | 고객 행동 데이터로 광고 문구·이미지 자동 생성 | `/service/creative` |
| 3 | AI 타겟 추천 | 고객 행동 데이터 분석으로 최적 타겟 자동 추천 | `/service/targeting` |
| 4 | 자동 리포트 | 일/주/월 성과 리포트 자동 생성·제공 | `/service/report` |
| 5 | 통합 플랫폼 관리 | Google·Meta·Naver·Kakao 한 번에 관리 | `/service/platform` |
| 6 | 실시간 알림 | 성과 변화·이상 징후 실시간 알림 | `/service/alert` |

카드: 흰 배경 + 라운드 + 그림자, hover 시 파란 테두리 + `translateY(-4px)`.

### 섹션 03 — 성과 지표(KPI) (배경 `#F0F4FF`)
제목: "검증된 성과로 성장을 가속화합니다" · CTA: `고객 성공 사례 보기 →`
카운트업(Intersection Observer 트리거, 재진입 시 재실행):

| 수치 | 항목 | 시작값 | 시간 |
|---|---|---|---|
| 1,000+ | 누적 고객사 | 0 | 2.0s |
| 50,000+ | 누적 캠페인 | 0 | 2.5s |
| 200% | 평균 ROAS 향상 | 0 | 2.0s |
| 30% | 업무 시간 절감 | 0 | 1.5s |
| 99.9% | 시스템 안정성 | 95 | 2.0s |

### 섹션 04 — 특허/기술 (5개 카드, 데스크톱 5열 / 모바일 2열)
제목: "독자적인 AI 기술과 특허로 차별화합니다" · CTA: `특허/기술 자세히 보기 →`
각 카드는 **아이콘 + 특허 제목 + "출원 예정" 배지** 만 노출한다 — **설명 카피는 넣지 않음**:
1) AI 예산 최적화 특허 2) AI 광고 소재 생성 특허 3) 성과 기반 수익 정산 특허 4) 멀티플랫폼 데이터 통합 특허 5) AI 에이전트 특허.

### Footer (4단)
컬럼1 로고+슬로건 / 컬럼2 회사정보 / 컬럼3 주소 / 컬럼4 바로가기+SNS.
- 회사명 에이아이툴즈(AI TOOLS) · 대표 이주호 · 사업자 728-81-04234
- 이메일 aitoolz@kakao.com · 대표전화 02-1234-5678 *(전화는 플레이스홀더, 추후 확정)*
- 주소 (우)10049 경기도 김포시 김포한강1로 51번길 18, 703-43호 (장기동, 신우프라자)
- 저작권 **© 2026 AI TOOLS Co., Ltd.** *(시안의 2024 → 2026 수정 반영)*
- 하단 링크 개인정보처리방침 | 이용약관 · SNS LinkedIn·YouTube

---

## 4. 기술 스택 (Tech Stack)

- **프레임워크**: Next.js (App Router, TypeScript)
- **스타일**: Tailwind CSS + shadcn/ui
- **애니메이션**: Framer Motion (페이드/순차), `react-countup` (KPI), Lottie (AI 칩 글로우 — 에셋 없으면 CSS 폴백)
- **패키지 매니저**: **pnpm**
- **배포 타깃**: Vercel
- **언어**: 한국어 단일 (i18n 미적용)

---

## 5. 명령어 (Commands)

```bash
pnpm install          # 의존성 설치
pnpm dev              # 개발 서버 (http://localhost:3000)
pnpm build            # 프로덕션 빌드
pnpm start            # 빌드 결과 실행
pnpm lint             # ESLint
pnpm typecheck        # tsc --noEmit
pnpm test             # 테스트 (Vitest)
pnpm exec playwright test   # E2E/스모크 (Playwright)
```

---

## 6. 프로젝트 구조 (Project Structure)

```
aitools/
├─ app/
│  ├─ layout.tsx           # 루트 레이아웃 (폰트·메타데이터)
│  ├─ page.tsx             # 메인 홈 (섹션 조립)
│  └─ globals.css          # Tailwind + CSS 변수
├─ components/
│  ├─ layout/              # Header(GNB), Footer
│  ├─ sections/            # Hero, AiDiagram(01), Features(02), Kpi(03), Patents(04)
│  ├─ ui/                  # shadcn 컴포넌트
│  └─ common/              # CountUp, SectionTag, AnimatedReveal 등
├─ lib/                    # 유틸 (cn 등)
├─ content/                # 카피·KPI·기능·특허 데이터 (배열 상수)
├─ public/                 # 로고·플랫폼 배지·정적 에셋
├─ tests/                  # 단위/컴포넌트 + e2e
├─ ref/                    # 기획 원본 (편집 금지)
└─ SPEC.md
```

원칙: 섹션별 콘텐츠(카피·수치)는 `content/`의 데이터로 분리해 JSX와 분리한다 (수정 용이·재사용).

---

## 7. 코드 스타일 (Code Style)

- TypeScript strict. `any` 지양, props 타입 명시.
- 함수형 컴포넌트 + 명시적 named export. 파일명 `PascalCase.tsx` (컴포넌트).
- Tailwind 우선, 색상은 테마 토큰(`text-primary` 등) 사용 — 하드코딩 hex 금지.
- **치수 단위는 rem (px 금지).** Tailwind 기본 스케일이 rem 기반이므로 유틸리티 우선 사용하고, 임의 값이 필요하면 `w-[22.5rem]`처럼 rem으로. 기획서의 px 수치(카드 360×220px, 아이콘 64px 등)는 **1rem=16px 기준으로 rem 환산**해 구현. (브라우저 루트 폰트 크기·접근성 대응)
- 클래스 병합은 `cn()` 유틸 사용.
- 접근성: 시맨틱 태그, 이미지 `alt`, 버튼/링크 구분, 키보드 포커스, 색 대비 WCAG AA.
- 반응형: 기획서 §10.2 변환 규칙 준수 (데스크톱/태블릿/모바일).
- 주변 코드 컨벤션을 따르고 불필요한 주석 자제.

---

## 8. 테스트 전략 (Testing Strategy)

마케팅 랜딩 특성상 **실용적 수준**으로 한다 (과도한 단위 테스트 지양):
- **정적 게이트(필수)**: `pnpm typecheck` + `pnpm lint` 무오류 = 모든 작업의 기본 통과 기준.
- **컴포넌트 테스트(Vitest + Testing Library)**: 로직 있는 컴포넌트 — KPI 카운트업(목표값 도달), 데모 폼 검증(필수 필드·이메일 형식), GNB 모바일 토글.
- **E2E 스모크(Playwright)**: 메인 페이지 렌더, 모든 섹션 존재, CTA 클릭 동작, 콘솔 에러 0.
- **빌드 검증**: `pnpm build` 성공.
- **수동 확인**: 데스크톱/모바일 반응형 + Lighthouse(성능·접근성) 점검 후 완료 처리.

---

## 9. 인수 기준 (Acceptance Criteria) — 1차

- [ ] `pnpm dev`로 메인 홈이 뜨고 Hero+01~04+Footer가 기획서 카피 그대로 노출.
- [ ] 색상·타이포가 §2 디자인 토큰과 일치.
- [ ] KPI 5종 카운트업이 스크롤 진입 시 동작(시작값·목표값·시간 일치).
- [ ] 기능 카드 6종 hover 효과, 특허 카드 5종 노출.
- [ ] GNB 스크롤 고정 + 모바일 햄버거 드로어 동작.
- [ ] 데모 신청 폼: 필수 필드 검증·이메일 형식 검증, 제출 시 성공 UI (전송 연동은 2차 표시).
- [ ] 데스크톱/태블릿/모바일 §10.2 레이아웃 규칙 충족.
- [ ] `typecheck`·`lint`·`build` 무오류, Playwright 스모크 통과.

---

## 10. 경계 (Boundaries)

**항상 한다 (Always)**
- 변경 전 SPEC을 먼저 갱신하고, 작은 단위로 증분 구현·검증.
- 디자인 토큰/확정 카피를 그대로 사용 (임의 변경 금지).
- 커밋 메시지·코드 한국어 카피 정확히 반영.

**먼저 묻는다 (Ask first)**
- 확정 카피·브랜드 컬러·정보 구조 변경.
- 외부 서비스/유료 의존성 추가(이메일 발송·analytics·CMS 등), 환경변수·시크릿 도입.
- 데모 폼 실제 전송 백엔드 연동 방식.
- 2차 페이지 본문 착수(이번 범위는 메인 랜딩).

**절대 안 한다 (Never)**
- `ref/` 원본 파일 수정/삭제.
- 가짜 플레이스홀더 수치를 사실처럼 단정 (KPI는 기획서 목표치임을 전제).
- 시크릿/키를 코드·저장소에 하드코딩.
- 사용자 승인 없는 배포(prod).

---

## 11. 미해결 / 추후 확정 (Open Items)

- 디자인 시안 PNG(`홈페이지_디자인_시안_260516.png`) 부재 → 텍스트 기획서 기반 제작.
- 대표전화 `02-1234-5678` 플레이스홀더 → 실제 번호 확정 필요.
- ~~이메일/도메인 불일치~~ → **해결**: 도메인 `aitoolz.kr`(non-www 정식), 이메일 `aitoolz@kakao.com` 확정. (기획서의 `aitools.ai`는 오타)
- 솔루션 소개서·특허 명세 PDF 에셋 미제공 → Secondary CTA/다운로드는 2차.
- AI 다이어그램 Lottie 에셋 미제공 → 1차는 CSS 글로우로 구현.
