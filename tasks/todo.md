# AI TOOLS 홈페이지 — 태스크 목록 (1차)

> 각 태스크: **목표 · 의존성 · 구현 · 인수기준(AC) · 검증**. 완료 시 체크.
> 공통 검증 게이트(모든 태스크): `pnpm typecheck` + `pnpm lint` 무오류.

---

## Phase 1 — Foundation

### [x] T1. 프로젝트 스캐폴딩 & 툴링 ✅ (⛳A 통과)
- **목표**: 실행 가능한 빈 Next.js 앱 + 디자인 토큰 + 파비콘 + 루트 레이아웃.
- **의존성**: 없음 (전제).
- **구현**:
  - Next.js(App Router, TS strict) + pnpm 초기화, ESLint, Prettier.
  - Tailwind 설치 + 테마 토큰 등록(`primary #1749A6`, `secondary #2D7DD2`, `ai-glow #00B4D8`, `bg-light #F0F4FF`, `text-dark #1A1A2E`, `border #E2E8F0`).
  - shadcn/ui 초기화(`button` 등 기본).
  - Vitest + Testing Library, Playwright 설정.
  - `assets/` → `app/icon.png`·`app/apple-icon.png`·`app/favicon.ico` 배치, `public/`에 로고 복사.
  - 루트 `layout.tsx`: 한글 폰트(Pretendard 등), metadata(title "AI TOOLS", description=슬로건, OG, favicon).
  - `lib/utils.ts`(cn), `content/` 폴더 골격.
- **AC**: `pnpm dev`로 `/` 렌더 / 탭에 파비콘·타이틀 / `bg-primary`가 #1749A6 / `pnpm build` 성공.
- **검증**: `typecheck`·`lint`·`build` 통과 + Playwright 스모크(페이지 로드, 콘솔 에러 0).
- **⛳ Checkpoint A**: 위 통과 후 다음 단계 진입.

---

## Phase 2 — Shell

### [x] T2. 헤더 / GNB ✅
- **목표**: 7개 메뉴 + 로그인/데모 CTA + 스크롤 동작 + 모바일 드로어.
- **의존성**: T1.
- **구현**: 좌 로고(`logo-horizontal-dark.png`), 중앙 7메뉴(홈·서비스·솔루션·특허/기술·회사소개·뉴스·문의하기), 우 `로그인`(텍스트)+`데모 신청`(파란 버튼). 스크롤 50px↑ → 흰 배경+그림자 고정. 모바일 햄버거 → 전체 드로어. 라우트는 플레이스홀더/앵커.
- **AC**: SPEC §3 GNB 구성 일치 / 스크롤 시 배경 전환 / 모바일 드로어 토글 / 키보드 포커스 가능.
- **검증**: 컴포넌트 테스트(모바일 토글 open/close) + 스모크(nav 노출).

### [x] T3. 푸터 ✅
- **목표**: 4단 푸터 + 회사정보 + SNS + 저작권.
- **의존성**: T1.
- **구현**: 컬럼1 로고(`logo-horizontal.png`, 다크 배경)+슬로건 / 컬럼2 회사명·대표 서동구·사업자 728-81-04234·이메일 aitoolz@kakao.com / 컬럼3 주소(우 10049, 김포…) / 컬럼4 바로가기+SNS. 저작권 **© 2026 AI TOOLS Co., Ltd.** + 개인정보처리방침·이용약관.
- **AC**: SPEC §3 푸터 일치 / 4단→2×2→1단 반응형.
- **검증**: 스모크(푸터 정보 텍스트 존재).

---

## Phase 3 — Sections

### [x] T4. Hero 섹션 ✅
- **의존성**: T1 (조립 시 T2).
- **구현**: 태그 `AI MARKETING OS` / 헤드라인 "AI가 광고의 성과를 분석하고 의사결정합니다"(`분석하고` 뒤 줄바꿈) / 서브카피 / CTA1 `데모 신청하기 →`(파란)·CTA2 `솔루션 소개서 다운로드 ↓`(흰+테두리) / 플랫폼 배지(Google Ads·Meta·NAVER·kakao) / 우측 AI 다이어그램 영역. fadeInUp.
- **AC**: 카피 정확 / 데스크톱 2단·모바일 1단(텍스트 상·이미지 하) / CTA1 클릭 → 데모 폼(T9) 연결.
- **검증**: 스모크(헤드라인·CTA 존재).

### [x] T5. 섹션 01 — AI 기능 다이어그램 ✅
- **의존성**: T1.
- **구현**: 중앙 'AI' 칩(파란+회로 글로우, CSS) + 6방향 방사형 카드(데이터 분석·예산 최적화·자동 실행·타겟팅 최적화·소재 생성·성과 리포트). 연결선 스크롤 진입 시 순차 페이드인(Framer Motion). 모바일 2열 그리드.
- **AC**: 6기능 배치·아이콘·서브텍스트 / 진입 애니메이션 / 반응형.
- **검증**: 스모크(6개 기능 라벨 존재).

### [x] T6. 섹션 02 — 핵심 기능 6종 ✅
- **의존성**: T1.
- **구현**: 제목 "AI가 모든 과정을 자동으로 처리합니다" / 3×2 카드 그리드(content 데이터) / hover 시 파란 테두리+`translateY(-4px)` / 각 카드 `자세히 보기` 링크(`/service/*` 플레이스홀더).
- **AC**: 6카드 카피·링크 일치 / hover 효과 / 3×2→2×3→1열 반응형.
- **검증**: 스모크(6카드) + (선택)hover 단위 확인.

### [x] T7. 섹션 03 — 성과 지표(KPI) ✅
- **의존성**: T1.
- **구현**: 배경 `#F0F4FF` / 좌측 제목 "검증된 성과로 성장을 가속화합니다"+서브+CTA `고객 성공 사례 보기 →` / 우측 KPI 5종 카운트업(react-countup, Intersection Observer 트리거): 1,000+ / 50,000+ / 200% / 30% / 99.9%(시작 95) — 각 시간·접미사 SPEC §7 준수.
- **AC**: 스크롤 진입 시 0→목표값 카운트업 / 시작값·단위 정확 / 반응형.
- **검증**: **컴포넌트 테스트**(카운트업 목표값 도달, 99.9% 시작 95) + 스모크.

### [x] T8. 섹션 04 — 특허/기술 ✅ (메인 페이지 조립 완료)
- **의존성**: T1.
- **구현**: 제목 "독자적인 AI 기술과 특허로 차별화합니다" / 5카드 = **아이콘 + 제목만**(배지·설명 없음): AI 예산 최적화·AI 광고 소재 생성·성과 기반 수익 정산·멀티플랫폼 데이터 통합·AI 에이전트 특허 / CTA `특허/기술 자세히 보기 →`. 데스크톱 5열·모바일 2열.
- **AC**: 5카드 제목 노출(배지·설명 없음) / 반응형.
- **검증**: 스모크(5카드 제목).
- **⛳ Checkpoint B**: `app/page.tsx`에 Hero+01~04+헤더+푸터 전체 조립 → 사용자 시각 리뷰.

---

## Phase 4 — Form

### [x] T9. 데모 신청 폼 (UI + 검증) ✅ (백엔드 전송은 2차)
- **의존성**: T1 (호출: T2 헤더 CTA, T4 Hero CTA).
- **구현**: 모달 또는 섹션 폼. 필드(회사명·담당자·이메일·연락처·문의내용·희망 일정). 클라이언트 검증(필수 필드, 이메일 형식). 제출 시 성공 UI. **백엔드 전송 없음**(2차 안내 표시).
- **AC**: 필수 검증·이메일 검증 동작 / 제출 시 성공 상태 / 접근성(label·focus).
- **검증**: **컴포넌트 테스트**(빈 제출 차단, 잘못된 이메일 차단, 정상 제출 성공).

---

## Phase 5 — Polish & Verify

### [x] T10. 반응형 · 접근성 · 애니메이션 폴리시 ✅
- **의존성**: T2~T9.
- **구현**: SPEC §10.2 반응형 규칙 전 섹션 점검 / 접근성(시맨틱·alt·focus·색대비 AA) / Framer Motion 진입 애니메이션·GNB 스크롤 일관화 / 색상은 토큰만 사용 확인.
- **AC**: 데스크톱/태블릿/모바일 레이아웃 규칙 충족 / 키보드 내비 / 명백한 a11y 이슈 없음.
- **검증**: 수동 반응형 + Lighthouse(성능·접근성) 점검.
- **반영 내역**:
  - `<main id="main-content">` 랜드마크 + "본문 바로가기" 스킵 링크(키보드 첫 포커스).
  - 모바일 드로어 `role="dialog"`/`aria-modal` + 포커스 관리(열 때 진입, 닫을 때 햄버거 복귀).
  - `prefers-reduced-motion`: `<MotionConfig reducedMotion="user">`(Framer Motion) + CSS 미디어쿼리(ping/bounce/spin).
  - `:focus-visible` 키보드 포커스 링(마우스 클릭엔 미표시).
  - 색 대비 AA: 옅은 회색 톤 상향(`/40·/45·/55·/60`→`/55~/70`), footer 다크 대비(`white/45`→`/60`), eyebrow `brand-mid`(4.2:1)→`brand`(8.3:1).
  - KPI 카운트업을 `enableScrollSpy`→motion `onViewportEnter`로 교체(콘솔 경고 제거).
  - 2차(미구현) 라우트 링크 `prefetch={false}`(404 prefetch·콘솔 노이즈 제거).
  - **검증 결과**: axe-core WCAG 2.0/2.1 A·AA **위반 0**(홈 데스크톱·모바일 390px·데모) / 프로덕션 콘솔 에러·실패 요청 0 / typecheck·lint·vitest 21·build·e2e 통과.

### [x] T11. 최종 검증 ✅ (1차 메인 랜딩 마감)
- **의존성**: 전체.
- **구현**: SPEC §9 인수기준 8항목 점검, 잔여 버그 수정.
- **AC**: §9 체크리스트 전부 충족.
- **검증**: `typecheck`·`lint`·`build` 무오류 + Playwright 스모크(전 섹션 존재·CTA 동작·콘솔 에러 0).
- **⛳ Checkpoint C**: 인수기준 충족 → 사용자 최종 리뷰 → (승인 시) 배포 단계(ship)로.
- **결과 (2026-06-21)**: §9 8항목 전부 충족, 게이트 전체 통과(vitest 21), axe WCAG A·AA 위반 0, 프로덕션 콘솔 0. 라이브: https://aitoolz.vercel.app. **1차 완료.**

---

## 2차 — 서브페이지 (진행 방식: 한 페이지씩 차례로 / 콘텐츠는 ref 기획서에서 추출)

> 현재 `/`·`/demo` 외 GNB·푸터 링크는 전부 404. ref 자료: 사업기획서·웹구현 기획안·홈페이지 구성안·특허명세서.

- [ ] S1. `/about` 회사소개 — 회사 개요·미션·연혁·팀 (ref 사업기획서)
- [x] S2. `/service` 서비스 — 플랫폼 소개·고민·작동 5단계·차별점·기대효과·활용분야 (ref 구성안) ✅
  - 재사용 `PageHero` 공통 컴포넌트 확립. 검증: axe A·AA 0, 콘솔 0, vitest 3건 추가, e2e 스모크.
  - [ ] (후속) `/service/*` 6개 상세 페이지 — 메인 기능 카드 링크 대상
- [x] S3. `/solution` 솔루션 — 산업별 솔루션(쇼핑몰·병원·학원·프랜차이즈) + 공통 기반 3종 (ref 기획서 F13) ✅
  - 공통 `SectionHeader` 추출(서비스·솔루션 공유). 검증: axe A·AA 0, 콘솔 0, vitest 3건, e2e 스모크.
- [ ] S4. `/patent` 특허/기술 — 특허 5종 상세 (ref 특허명세서)
- [ ] S5. `/contact` 문의하기 — 문의 폼 + 회사정보/지도
- [ ] S6. `/news` 뉴스 — 목록 골격
- [ ] S7. `/login` 로그인 — UI만 (인증 백엔드 추후)
- [ ] S8. `/privacy`·`/terms` — 약관 텍스트
- [ ] S9. 데모 폼 백엔드 연동 (Formspree 또는 Resend → aitoolz@kakao.com) — **맨 마지막**
